---
layout: default
title: Project layout
nav_order: 3
has_children: true
---

# Project layout

`aicr` operates on a **project directory** — a self-contained folder that holds all configuration, input, and output for a study.

- **[Project directory](project-layout/project-directory)** — directory tree, file list, and what to commit vs gitignore
- **[Configuration files](project-layout/configuration)** — `project.json`, `experiment.json`, `deployments.json`, `endpoints.json`, `secrets.json`, and deployment modes
